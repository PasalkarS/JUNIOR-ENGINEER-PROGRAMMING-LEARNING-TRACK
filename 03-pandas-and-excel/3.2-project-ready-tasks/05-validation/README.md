# 05 — Data Validation & Audit Reports

## Objective
Build an automated data quality pipeline that flags constraint violations (null keys, negative quantities, zero pricing, duplicates) and generates structured audit reports.

## How to Run
```bash
python data_quality_auditor.py
```
