# 06 — Performance & Vectorization

## Objective
Demonstrate the massive speedup of columnar vectorization over row-by-row Python loops (`iterrows()`), and optimize RAM consumption using datatype downcasting.

## How to Run
```bash
python vectorization_benchmark.py
```
