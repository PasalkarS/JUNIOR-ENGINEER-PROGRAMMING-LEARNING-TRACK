# 04 — Interfaces and Abstraction

## Objective
Define an abstract storage contract (`IUserRepository`) using Python's `abc.ABC` and build swappable implementations.

## How to Run
```bash
python repository_interfaces.py
```
