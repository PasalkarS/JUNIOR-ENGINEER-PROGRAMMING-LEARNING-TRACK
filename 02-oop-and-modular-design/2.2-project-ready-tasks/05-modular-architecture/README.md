# 05 — Modular Architecture

## Objective
Convert a single monolithic script into a clean layered architecture:
- `models.py`: Domain entity definitions
- `repository.py`: Storage management
- `service.py`: Business workflow logic and validations
- `app.py`: Application driver and wiring

## How to Run
```bash
python app.py
```
