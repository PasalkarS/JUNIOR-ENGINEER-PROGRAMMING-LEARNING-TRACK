# 03 — Inheritance and Composition

## Objective
Demonstrate the practical difference between subclass inheritance (`BaseNotifier`) and object composition (`AlertManager`).

## Architectural Takeaway
Inheritance establishes an "is-a" relationship, while composition establishes a "has-a" relationship. Composition allows dynamic assembly and easier unit testing.

## How to Run
```bash
python notification_patterns.py
```
